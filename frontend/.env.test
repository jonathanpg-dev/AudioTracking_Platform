# Loaded automatically by Vitest (mode=test). Not a real backend -- every test either mocks
# fetch via MSW (see src/test/mswServer.ts) or doesn't make network requests at all.
VITE_API_BASE_URL=http://localhost:8080
VITE_GOOGLE_CLIENT_ID=test-google-client-id
